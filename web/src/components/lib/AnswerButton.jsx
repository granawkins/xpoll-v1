import { styled, Button } from "@mui/material"

const AnswerButton = styled(Button)({
    m: 0,
    p: 0,
    width: '100%',
    outline: '2px solid #9FD6FB',
    textTransform: 'none',
    borderRadius: 15,
    variant: "outlined",
    color: '#7CA6C1',
    height: '100%',
})

export default AnswerButton
